# Redpanda Load Test Environment Variables Template
# FOR INTERNAL/ON-PREMISE REDPANDA CLUSTERS
# Copy this file to your environment file and fill in your values

# Internal Broker Configuration
REDPANDA_BROKERS=10.1.0.56:9092,10.1.1.250:9092,10.1.2.91:9092

# Authentication credentials (same for producer and consumer)
REDPANDA_USER=loadtest
REDPANDA_PASS=your-password-here

# For internal clusters that require authentication, set these to auto-detect SASL
# (These are automatically detected when REDPANDA_USER and REDPANDA_PASS are provided)
# REDPANDA_SASL_ENABLED=true

# If your internal cluster uses TLS, uncomment this:
# REDPANDA_TLS_ENABLED=true

# Usage:
# 1. Copy this file: cp redpanda.internal.env.template my-cluster.env
# 2. Update the broker IPs and credentials above
# 3. Source the file: source my-cluster.env
# 4. Run: ./run_loadtest_only.sh
